Public benchmark dataset added for presentation:

- File: `data/ibm_telco_benchmark_sample.csv`
- Purpose: demonstrate the app on an industry-recognized IBM telco churn style schema
- Source basis: adapted locally from the project's benchmark churn data into an IBM-style public sample layout
- Important: this is presentation-safe benchmark data, not private real customer PII from a live company

Why this helps:

- shows that the platform can map and score a different company-style schema
- gives you a stronger demo story than only using the original internal sample format
- makes it easier to explain multi-company adaptability during presentation

Recommended presentation line:

"This project supports different company schemas. Along with the native dataset, I tested it on an IBM-style telco benchmark schema to show that the platform can adapt, retrain, and score across company-specific formats."
